import "./Stack.scss"

const StackButton = (props) => {
    return (
        // <div>
             <button className="tech">{props.tech}</button>
        // </div>
    );
}

export default StackButton;